# Scout report (planning input)

See conversation scout from explore agent (2026-09-15). Key gaps:

- No `COOKIE_DOMAIN` / `AUTH_COOKIE_DOMAIN` yet
- No Vite `/api` proxy on demo-react / landing for backend-only DX
- `safeNext` shell-only; remote `main.tsx` has no SessionGate
- Seeder still `demo` / `demoReact` / `./App`
